<%- include('../partials/header') %>

<h1 id="my-surveys">My Surveys</h1>
<table>
    <thead>
        <tr>
            <th>Subject</th>
            <th>Comment</th>
            <th>Rating</th>
            <th>Case Number</th>
        </tr>
    </thead>
    <tbody>
        <% cases.forEach(c => { %>
            <% if(c.survey){ %>
                <tr>
                    <td><%= c.subject %></td>
                    <td id="comment"><%= c.survey.comment %></td>
                    <td id="rating"><%= c.survey.rating %></td>
                    <td><%= c.caseNum %></td>
                    <!-- <td><a id="edit" 
onclick="document.getElementById('comment').innerHTML = '<textarea></textarea>', document.getElementById('rating').innerHTML= ' <select><option selected>Select A Rating</option><option>1</option><option>2</option><option>3</option><option>4</option><option>5</option></select>'"                         
                        >Edit</a></td> -->
                    <!-- <td><a href="/surveys/<%= c._id %>/update">Save</a></td>
                    <td><a href="/surveys/mysurveys">Cancel</a></td> -->
                </tr>
            <% } %>
        <% }) %>
    </tbody>

<%- include('../partials/footer') %>